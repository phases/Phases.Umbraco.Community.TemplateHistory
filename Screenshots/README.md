# Screenshots

Add marketplace screenshots here before publishing the repository:

- `history-workspace.png` — History tab with version list
- `diff-comparison.png` — Side-by-side diff comparison
- `settings-dashboard.png` — Settings → Template History dashboard

These paths are referenced in `umbraco-marketplace.json` and `README.md`.
